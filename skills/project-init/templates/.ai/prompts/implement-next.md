<!-- Provenance: bundled project-init template.
     Routing index: .ai/prompts/README.md. -->

# Implement the Next Approved Task

Use `implement-next` with the exact approved PLAN path.

Select only the first unchecked dependency-ordered task. Use the relevant
`backend-feature` or `frontend-feature` skill for application work, and use TDD
for testable behavior. Preserve scope, inspect before editing, protect secrets,
and do not commit, push, deploy, or start another task.

Run fresh validation, update the task evidence and session handoff, and stop.
